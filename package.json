{
  "name": "tech-matchup-app",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js --ignore dogtraining",
    "develop": "concurrently \"cd server && npm run watch\" \"cd dogtraining && npm run dev\"",
    "install": "cd server && npm i && cd ../dogtraining && npm i",
    "seed": "cd server && npm run seed",
    "build": "cd dogtraining && npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
